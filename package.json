{
  "name": "indexed-subgraph",
  "version": "0.1.0",
  "scripts": {
    "compile": "truffle compile",
    "build-contract": "solc contracts/MarketOracle.sol --abi -o abis --overwrite && solc contracts/MarketOracle.sol --bin -o bin --overwrite",
    "create": "graph create indexed-finance/indexed-subgraph --node https://api.thegraph.com/deploy/",
    "create-local": "graph create indexed-finance/indexed-subgraph --node http://127.0.0.1:8020",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy indexed-finance/indexed-subgraph --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy-local": "graph deploy indexed-finance/indexed-subgraph --ipfs http://localhost:5001 --node http://127.0.0.1:8020"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.18.0",
    "@graphprotocol/graph-ts": "^0.18.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0",
    "truffle": "^5.1.42",
    "truffle-contract": "^4.0.31",
    "truffle-hdwallet-provider": "^1.0.4"
  }
}
